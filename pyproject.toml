[project]
name = "brain-404"
version = "0.1.0"
description = "Wired Al LLMOps project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["backend", "frontend"]

[dependency-groups]
dev = ["ipykernel>=7.2.0", "pytest", "ruff"]
